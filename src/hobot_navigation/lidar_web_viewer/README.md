# lidar_web_viewer

`lidar_web_viewer` subscribes to `/scan` and starts a small HTTP server.

Open the browser with:

```bash
http://<car-ip>:8765
```

Launch:

```bash
ros2 launch lidar_web_viewer lidar_web_viewer.launch.py
```
