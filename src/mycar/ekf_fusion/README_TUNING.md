# EKF tuning notes

调参顺序建议固定下来：

1. 检查轮速方向、单位和舵机标定；
2. 检查 IMU 轴向、零偏、时间戳和静止状态；
3. 先只融合轮速与 IMU，确认 `/odom` 连续；
4. 再用外部雷达里程计做离线 A/B 对比；
5. 最后才调整过程噪声、观测协方差和异常观测阈值。

仓库里的 `tools/analyze_ekf_bags.py` 可以读取用户自己准备的 rosbag 做统计。数据文件
不随仓库发布，路径通过命令行传入。静态地图和导航规划不属于 EKF 的职责。
