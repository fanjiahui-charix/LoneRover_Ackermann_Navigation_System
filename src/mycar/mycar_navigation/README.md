# mycar_navigation

这是与具体比赛任务无关的车体运动与局部导航工具包，包含 Ackermann 车辆几何、局部
栅格、锥桶聚类辅助、曲率限速、制动距离和速度平滑相关代码。

它接收标准 ROS 2 话题和参数，不依赖二维码、相机、大模型、BEV 或厂商消息。任务层
可以把普通 `Twist`/路径目标交给它，再由上层导航入口统一做速度和安全约束。

虚拟车模与离线调参工具在根目录 `tools/` 和 `origincar_description` 中；这里的代码
适合单元测试和作为 Nav2 控制器外的车辆约束参考。
